# Quickstart

Before looking at all the options, let's do a quick demonstration of the application.

=== "Docker Compose"

    ```bash
    docker compose
    ```

=== "Docker"

    ```bash
    docker ...
    ```
